
const Score = (props) => {
  // const [score, setScore] = useState(0);

  // const increaseScore = () => {
  //   setScore(score +1);
  // }

  return (
    <h3>Score: {props.score}</h3>
  )
}

export default Score;